import React from 'react';

type Props = {
    className?: string;
    size?: number;
    fill?: string;
};
declare const VerifiedBadge: {
    ({ className, size, fill }: Props): React.JSX.Element;
    className: string;
    checkMarkClassName: string;
};

export { VerifiedBadge as default };

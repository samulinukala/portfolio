import { IUser } from '@giphy/js-types';
import React from 'react';

type Props = {
    user: IUser;
    className?: string;
};
declare const Avatar: ({ user, className }: Props) => React.JSX.Element;

export { Avatar as default };

import { IGif } from '@giphy/js-types';
import React from 'react';

type Props = {
    gif: IGif;
    className?: string;
    onClick?: (gif: IGif) => void;
};
declare const Attribution: {
    ({ gif, className, onClick }: Props): React.JSX.Element | null;
    className: string;
};

export { Attribution as default };

import React, { SyntheticEvent } from 'react';

type Props = {
    onClick: (e: SyntheticEvent<HTMLElement, Event>) => void;
};
declare const FetchError: ({ onClick }: Props) => React.JSX.Element;

export { FetchError as default };

"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/components/masonry-grid.tsx
var masonry_grid_exports = {};
__export(masonry_grid_exports, {
  default: () => masonry_grid_default
});
module.exports = __toCommonJS(masonry_grid_exports);
var import_react = __toESM(require("react"));
function fillArray(length, columnOffsets = []) {
  return Array.apply(null, Array(length)).map((_, index) => columnOffsets[index] || 0);
}
var MasonryGrid = ({
  columns,
  gutter,
  useTransform = true,
  itemWidth,
  itemHeights,
  children,
  columnOffsets = []
}) => {
  const containerStyle = {};
  function getChildren() {
    let columnTarget;
    const columnHeights = fillArray(columns, columnOffsets);
    const result = import_react.default.Children.map(children, (child, index) => {
      const style = {
        position: "absolute"
      };
      columnTarget = columnHeights.indexOf(Math.min.apply(Math, columnHeights));
      const top = `${columnHeights[columnTarget]}px`;
      const left = `${columnTarget * itemWidth + columnTarget * gutter}px`;
      if (useTransform) {
        style.transform = `translate3d(${left}, ${top}, 0)`;
      } else {
        style.top = top;
        style.left = left;
      }
      const height = itemHeights[index];
      if (height) {
        columnHeights[columnTarget] += height + gutter;
      }
      return import_react.default.cloneElement(child, { style });
    });
    containerStyle.position = "relative";
    containerStyle.width = `${columns * itemWidth + (columns - 1) * gutter}px`;
    containerStyle.height = `${Math.max.apply(Math, columnHeights) - gutter}px`;
    return result;
  }
  return /* @__PURE__ */ import_react.default.createElement("div", { style: containerStyle }, getChildren());
};
var masonry_grid_default = (0, import_react.memo)(MasonryGrid);

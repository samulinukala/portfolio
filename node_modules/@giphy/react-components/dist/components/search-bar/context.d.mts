import { GifsResult, SearchOptions } from '@giphy/js-fetch-api';
import { IChannel } from '@giphy/js-types';
import React, { ReactNode } from 'react';

type SearchContextProps = {
    setSearch: (searchTerm: string) => void;
    term: string;
    activeChannel: IChannel | undefined;
    setChannels: (channels: IChannel[]) => void;
    currentChannels: IChannel[];
    setActiveChannel: (channel: IChannel | undefined) => void;
    fetchGifs: (offset: number) => Promise<GifsResult>;
    fetchAnimatedText: (offset: number) => Promise<GifsResult>;
    fetchChannelSearch: (offset: number) => Promise<IChannel[]>;
    isFetching: boolean;
    trendingSearches: string[];
    searchKey: string;
    isFocused: boolean;
};
type _SearchContextProps = {
    setIsFocused: (focused: boolean) => void;
    _setSearch: (searchTerm: string) => void;
    _inputValOverride: string;
};
declare const CssVars: {
    searchbarHeight: string;
    bgColor: string;
    bgColor2: string;
    fgColor: string;
    cancelButtonDisplay: string;
};
declare const SearchContext: React.Context<SearchContextProps>;
declare const _SearchContext: React.Context<_SearchContextProps>;
type Props = {
    children: ReactNode;
    options?: SearchOptions;
    apiKey: string;
    initialTerm?: string;
    initialChannel?: IChannel;
    shouldDefaultToTrending?: boolean;
    shouldFetchChannels?: boolean;
    theme?: {
        darkMode?: boolean;
        searchbarHeight?: number;
        hideCancelButton?: boolean;
        mobileMediaQuery?: string;
        mobileSearchbarHeight?: number;
    };
};
declare const SearchContextManager: ({ children, options, apiKey, initialTerm, initialChannel, shouldDefaultToTrending, shouldFetchChannels, theme, }: Props) => React.JSX.Element;

export { CssVars, SearchContext, type SearchContextProps, _SearchContext, type _SearchContextProps, SearchContextManager as default };

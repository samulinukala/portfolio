import React from 'react';

type Props = {
    className?: string;
    placeholder?: string;
    clear?: boolean;
    autoFocus?: boolean;
    searchDebounce?: number;
    initialTerm?: string;
    onEnter?: (term: string) => void;
};
declare const SearchBar: {
    ({ className, placeholder, clear, autoFocus, searchDebounce, onEnter, }: Props): React.JSX.Element;
    className: string;
};

export { SearchBar as default };

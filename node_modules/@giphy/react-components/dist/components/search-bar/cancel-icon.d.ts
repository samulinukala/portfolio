import React from 'react';

type Props = {
    width?: number;
    height?: number;
    setCleared: (clear: boolean) => void;
};
declare const CancelIcon: {
    ({ width, height, setCleared }: Props): React.JSX.Element | null;
    className: string;
};

export { CancelIcon as default };

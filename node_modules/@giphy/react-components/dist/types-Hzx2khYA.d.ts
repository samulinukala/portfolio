import { IGif } from '@giphy/js-types';

type GifOverlayProps = {
    gif: IGif;
    isHovered: boolean;
    onClick?: (gif: IGif) => void;
    width?: number;
    height?: number;
};

export type { GifOverlayProps as G };

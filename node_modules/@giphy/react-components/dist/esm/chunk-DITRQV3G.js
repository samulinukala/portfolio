import {
  clips_branding_default
} from "./chunk-EN3EGUGS.js";
import {
  avatar_default
} from "./chunk-Y2GBD252.js";
import {
  user_default
} from "./chunk-JNXOCL5L.js";

// src/components/video/attribution.tsx
import React from "react";
import styled from "styled-components";
var Container = styled.div`
    display: flex;
    align-items: center;
    font-family: interface, helvetica, arial;
    cursor: pointer;
`;
var Avatar = styled(avatar_default)`
    flex-shrink: 0;
`;
var Right = styled.div`
    display: flex;
    flex-direction: column;
`;
var Attribution = ({ gif, className, onClick }) => {
  const { user } = gif;
  if (!(user == null ? void 0 : user.username) && !(user == null ? void 0 : user.display_name)) {
    return null;
  }
  return /* @__PURE__ */ React.createElement(
    Container,
    {
      className: [Attribution.className, className].join(" "),
      onClick: (e) => {
        e.preventDefault();
        e.stopPropagation();
        if (onClick) {
          onClick(gif);
        } else {
          const url = user.profile_url;
          if (url)
            window.open(url, "_blank");
        }
      }
    },
    /* @__PURE__ */ React.createElement(Avatar, { user }),
    /* @__PURE__ */ React.createElement(Right, null, /* @__PURE__ */ React.createElement(
      clips_branding_default,
      {
        onClick: (e) => {
          e.preventDefault();
          e.stopPropagation();
          if (onClick) {
            onClick(gif);
          } else if (gif.url) {
            window.open(gif.url, "_blank");
          }
        }
      }
    ), /* @__PURE__ */ React.createElement(user_default, { user }))
  );
};
Attribution.className = "giphy-attribution";
var attribution_default = Attribution;

export {
  attribution_default
};

import {
  verified_badge_default
} from "./chunk-QKMOCXMK.js";

// src/components/attribution/user.tsx
import React from "react";
import styled from "styled-components";
var Username = styled.div`
    color: white;
    font-size: 16px;
    font-weight: 700;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    -webkit-font-smoothing: antialiased;
`;
var VerifiedBadge = styled(verified_badge_default)`
    margin-left: 4px;
    flex-shrink: 0;
`;
var Container = styled.div`
    display: flex;
    align-items: center;
    min-width: 0;
`;
var User = ({ user }) => {
  const { display_name, username } = user;
  return /* @__PURE__ */ React.createElement(Container, null, /* @__PURE__ */ React.createElement(Username, null, display_name || `@${username}`), user.is_verified ? /* @__PURE__ */ React.createElement(VerifiedBadge, { size: 14 }) : null);
};
var user_default = User;

export {
  Username,
  user_default
};

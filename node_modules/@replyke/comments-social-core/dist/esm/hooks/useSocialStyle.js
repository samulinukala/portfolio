import { useEffect, useState } from "react";
import { socialBaseStyle } from "../social-base-style";
import { mergeSocialStyleData } from "../helpers/mergeSocialStyleData";
function useSocialStyle(props) {
    const [styleConfig, setStyleConfig] = useState(socialBaseStyle);
    useEffect(() => {
        const mergedStyle = mergeSocialStyleData(props?.commentFeedProps, props?.commentProps, props?.newCommentFormProps);
        setStyleConfig(mergedStyle);
    }, [props]);
    return styleConfig;
}
export default useSocialStyle;
//# sourceMappingURL=useSocialStyle.js.map
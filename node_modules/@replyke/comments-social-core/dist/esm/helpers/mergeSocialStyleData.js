import { safeMergeStyleProps } from "@replyke/ui-core";
import { socialBaseStyle } from "../social-base-style";
export function mergeSocialStyleData(commentFeedProps, commentProps, newCommentFormProps) {
    const mergedStyle = {
        type: socialBaseStyle.type,
        commentFeedProps: safeMergeStyleProps(socialBaseStyle.commentFeedProps, commentFeedProps),
        commentProps: safeMergeStyleProps(socialBaseStyle.commentProps, commentProps),
        newCommentFormProps: safeMergeStyleProps(socialBaseStyle.newCommentFormProps, newCommentFormProps),
    };
    return mergedStyle;
}
//# sourceMappingURL=mergeSocialStyleData.js.map
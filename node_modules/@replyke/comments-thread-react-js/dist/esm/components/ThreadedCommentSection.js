import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useThreadedStyle, } from "@replyke/comments-threaded-core";
import useThreadedComments from "../hooks/useThreadedComments";
import { CommentsFeed } from "./CommentsFeed";
import { NewCommentForm } from "./NewCommentForm";
function SocialCommentSection({ entity, entityId, foreignId, shortId, callbacks, styleConfig: styleConfigProp, isVisible = true, }) {
    const styleConfig = useThreadedStyle(styleConfigProp);
    const { CommentSectionProvider } = useThreadedComments({
        entity,
        entityId,
        foreignId,
        shortId,
        styleConfig,
        callbacks,
    });
    return (_jsx(CommentSectionProvider, { children: _jsxs("div", { style: { height: "100%", display: "flex", flexDirection: "column" }, children: [_jsx("div", { style: {
                        flex: 1,
                        overflowY: "auto",
                        backgroundColor: "white",
                    }, children: _jsx(CommentsFeed, {}) }), _jsx("div", { style: { borderTop: "1px solid #e5e7eb" }, children: isVisible && _jsx(NewCommentForm, {}) })] }) }));
}
export default SocialCommentSection;
//# sourceMappingURL=ThreadedCommentSection.js.map
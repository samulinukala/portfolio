import { AxiosInstance } from "axios";
export interface ClientConfig {
    projectId: string;
}
export declare class ReplykeHttpClient {
    instance: AxiosInstance;
    constructor({ projectId }: ClientConfig);
}

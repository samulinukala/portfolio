import { ReplykeHttpClient } from "../../core/client";
export interface FetchUserByForeignIdProps {
    foreignId: string;
    name?: string;
    username?: string;
    avatar?: string;
    bio?: string;
    metadata?: Record<string, any>;
    secureMetadata?: Record<string, any>;
}
export declare function fetchUserByForeignId(client: ReplykeHttpClient, data: FetchUserByForeignIdProps): Promise<any>;

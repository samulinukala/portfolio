import { ReplykeHttpClient } from "../../core/client";
export interface FetchUserByIdProps {
    userId: string;
}
export declare function fetchUserById(client: ReplykeHttpClient, data: FetchUserByIdProps): Promise<any>;

import { ReplykeHttpClient } from "../../core/client";
export interface UpdateEntityProps {
    entityId: string;
    title?: string;
    content?: string;
    attachments?: Record<string, any>[];
    keywords?: string[];
    location?: {
        type: "Point";
        coordinates: [number, number];
    };
    metadata?: Record<string, any>;
    mentions?: {
        id: string;
        username: string;
    }[];
}
export declare function updateEntity(client: ReplykeHttpClient, data: UpdateEntityProps): Promise<any>;

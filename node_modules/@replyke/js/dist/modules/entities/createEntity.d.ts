import { ReplykeHttpClient } from "../../core/client";
export interface CreateEntityProps {
    foreignId?: string;
    sourceId?: string;
    title?: string;
    content?: string;
    attachments?: Record<string, any>[];
    keywords?: string[];
    location?: {
        latitude: number;
        longitude: number;
    };
    metadata?: Record<string, any>;
    userId?: string;
}
export declare function createEntity(client: ReplykeHttpClient, data: CreateEntityProps): Promise<any>;

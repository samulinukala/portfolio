import { ReplykeHttpClient } from "../../core/client";
export interface FetchEntityProps {
    entityId: string;
}
export declare function fetchEntity(client: ReplykeHttpClient, data: FetchEntityProps): Promise<any>;

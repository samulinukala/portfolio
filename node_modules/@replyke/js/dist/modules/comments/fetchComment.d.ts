import { ReplykeHttpClient } from "../../core/client";
export interface FetchCommentProps {
    commentId: string;
}
export declare function fetchComment(client: ReplykeHttpClient, data: FetchCommentProps): Promise<any>;

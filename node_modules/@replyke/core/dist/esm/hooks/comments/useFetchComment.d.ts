import { Comment } from "../../interfaces/models/Comment";
declare function useFetchComment(): ({ commentId, withParent, }: {
    commentId: string;
    withParent?: boolean;
}) => Promise<{
    comment: Comment;
    parentComment: Comment | null;
}>;
export default useFetchComment;

import { Comment } from "../../interfaces/models/Comment";
declare function useUpdateComment(): ({ commentId, content, metadata, }: {
    commentId: string;
    content?: string;
    metadata?: Record<string, any>;
}) => Promise<Comment>;
export default useUpdateComment;

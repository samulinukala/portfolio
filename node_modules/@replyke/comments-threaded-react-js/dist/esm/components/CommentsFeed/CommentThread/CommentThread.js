import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useEffect, useMemo, useState } from "react";
import { useCommentSection, useReplies, } from "@replyke/react-js";
import { SingleComment } from "./SingleComment";
import CommentReplies from "./CommentReplies";
function CommentThread({ comment, depth, isLastReply = false, }) {
    const [isCollapsed, setIsCollapsed] = useState(false);
    const [showAllReplies, setShowAllReplies] = useState(false);
    const { sortBy } = useCommentSection();
    const { replies, newReplies, loading, page, setPage } = useReplies({
        commentId: comment.id,
        sortBy: sortBy || "new", // Add fallback instead of assertion
    });
    useEffect(() => {
        // Reset the page when the comment changes
        setPage(1);
    }, [setPage]);
    const initialVisibleReplies = 3;
    const allReplies = useMemo(() => {
        const combined = [...(newReplies || []), ...(replies || [])];
        return combined;
    }, [newReplies, replies, comment.content]);
    const visibleReplies = useMemo(() => {
        const visible = showAllReplies
            ? allReplies
            : allReplies.slice(0, initialVisibleReplies);
        return visible;
    }, [showAllReplies, allReplies, initialVisibleReplies, comment.content]);
    const hiddenRepliesCount = Math.max(0, allReplies.length - initialVisibleReplies);
    const handleToggleCollapse = () => {
        setIsCollapsed(!isCollapsed);
    };
    const handleShowAllReplies = () => {
        setShowAllReplies(true);
    };
    const hasReplies = allReplies.length > 0;
    const replyCount = allReplies.length;
    return (_jsxs(_Fragment, { children: [_jsx(SingleComment, { comment: comment, depth: depth, hasReplies: hasReplies, isCollapsed: isCollapsed, replyCount: replyCount, isLastReply: isLastReply, onToggleCollapse: handleToggleCollapse }), _jsx(CommentReplies, { depth: depth, isCollapsed: isCollapsed, loading: loading, visibleReplies: visibleReplies, hiddenRepliesCount: hiddenRepliesCount, showAllReplies: showAllReplies, onShowAllReplies: handleShowAllReplies, CommentThreadComponent: CommentThread })] }));
}
export default CommentThread;
//# sourceMappingURL=CommentThread.js.map
import { useEffect, useState } from "react";
import { threadedBaseStyle } from "../threaded-base-style";
import { mergeThreadedStyleData } from "../helpers/mergeThreadedStyleData";
function useSocialStyle(props) {
    const [styleConfig, setStyleConfig] = useState(threadedBaseStyle);
    useEffect(() => {
        const mergedStyle = mergeThreadedStyleData(props?.commentFeedProps, props?.commentProps, props?.newCommentFormProps);
        setStyleConfig(mergedStyle);
    }, [props]);
    return styleConfig;
}
export default useSocialStyle;
//# sourceMappingURL=useThreadedStyle.js.map